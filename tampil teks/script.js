const button = document.getElementById('btn')
const input = document.getElementById('masukan')

button.addEventListener('click', () => {
    const input = masukan.value 

    hasil.textContent = `${input}`
})